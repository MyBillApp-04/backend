# name: Backend CI

# on:
#   pull_request:
#   push:
#     branches: [main]

# permissions:
#   contents: read

# jobs:
#   test:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-java@v4
#         with:
#           distribution: temurin
#           java-version: '17'
#           cache: maven
#       - name: Run unit and integration tests
#         run: ./mvnw -B test
#       - name: Build deployable JAR
#         run: ./mvnw -B -DskipTests package

#   postgres-migration-smoke:
#     runs-on: ubuntu-latest
#     services:
#       postgres:
#         image: postgres:17-alpine
#         env:
#           POSTGRES_DB: mybill
#           POSTGRES_USER: mybill
#           POSTGRES_PASSWORD: mybill
#         ports:
#           - 5432:5432
#         options: >-
#           --health-cmd "pg_isready -U mybill -d mybill"
#           --health-interval 10s --health-timeout 5s --health-retries 5
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-java@v4
#         with:
#           distribution: temurin
#           java-version: '17'
#           cache: maven
#       - name: Start against PostgreSQL and apply Flyway migrations
#         env:
#           SPRING_PROFILES_ACTIVE: prod
#           DB_URL: jdbc:postgresql://localhost:5432/mybill
#           DB_USERNAME: mybill
#           DB_PASSWORD: mybill
#           JWT_SECRET: ci-only-secret-must-never-be-used-in-production-123456789
#           FIREBASE_CONFIG_JSON: ''
#           STRIPE_SECRET_KEY: ''
#           STRIPE_WEBHOOK_SECRET: ''
#           PORT: 8080
#         run: |
#           ./mvnw -B -DskipTests spring-boot:run > application.log 2>&1 &
#           APP_PID=$!
#           for i in {1..45}; do
#             if grep -q "Started MyBillBackendApplication" application.log; then
#               kill "$APP_PID" || true
#               wait "$APP_PID" || true
#               exit 0
#             fi
#             if ! kill -0 "$APP_PID" 2>/dev/null; then break; fi
#             sleep 2
#           done
#           cat application.log
#           kill "$APP_PID" || true
#           exit 1
